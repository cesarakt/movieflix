spring:
  application:
    name: movieflix

  datasource:
    url: jdbc:postgresql://localhost:5432/db_movieflix
    username: admin
    password: 130797Cesar
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
